export enum WebinarSessionStatus {
    SCHEDULED = 'scheduled',
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
    CANCELED = 'canceled'
}

export enum WebinarSeriesStatus {
    SCHEDULED = 'scheduled',
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
    CANCELED = 'canceled'
}

export enum WebinarSeriesType {
    SINGLE = 'single',
    MULTI = 'multi',
    RECURRING = 'recurring'
}